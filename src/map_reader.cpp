#include <fstream>
#include <iostream>
#include <string>

#include "map/map.h"

Map* read_map_file(const std::string path, Map &map){
  std::ifstream file;

  Map::MapBuilder builder;
  return builder.build();
}
